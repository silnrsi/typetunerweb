apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: typetunerweb
  namespace: wstech
spec:
  rules:
  - host: typetuner.languagetechnology.org
    http:
      paths:
      - path: /ttw
        pathType: Prefix
        backend:
          service:
            name: typetunerweb
            port:
              name: http
